export interface User { // Aqui, estamos definindo uma interface chamada User que representa um usuário
    id: string;
    name: string;
    phone: string;
    email: string;
    password: string;
    createdAt: string;
}
